/* eslint-disable no-dupe-keys */
// PUT '/' for a get request route only.
// import Config from 'react-native-config';

export default {
  // BASE_URL: Config.API_URL,
  BASE_URL: 'https://api.publicapis.org',
  HOME: '/random',
};
